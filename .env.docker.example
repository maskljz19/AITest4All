# Docker Compose Environment Variables
# Copy this file to .env and configure your settings

# ==========================================
# Database Configuration
# ==========================================
POSTGRES_PASSWORD=your_secure_password_here

# ==========================================
# LLM API Keys (REQUIRED)
# ==========================================
# At least one of these is required for the system to work
OPENAI_API_KEY=sk-your-openai-api-key-here
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# ==========================================
# Security
# ==========================================
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=your_base64_encryption_key_here

# ==========================================
# LLM Configuration
# ==========================================
# Custom API endpoints (optional, for proxies or self-hosted models)
OPENAI_API_BASE=https://api.openai.com/v1
ANTHROPIC_API_BASE=https://api.anthropic.com

# Default model settings
DEFAULT_MODEL_PROVIDER=openai
DEFAULT_MODEL_NAME=gpt-4
DEFAULT_TEMPERATURE=0.7
DEFAULT_MAX_TOKENS=2000

# ==========================================
# Application Settings
# ==========================================
APP_WORKERS=4
SESSION_EXPIRE_HOURS=24
MAX_UPLOAD_SIZE_MB=10
SCRIPT_TIMEOUT_SECONDS=30
LOG_LEVEL=INFO

# ==========================================
# CORS Configuration
# ==========================================
# Add your domain(s) here for production
CORS_ORIGINS=http://localhost,http://localhost:80,https://your-domain.com

# ==========================================
# Notes:
# ==========================================
# 1. Never commit the .env file to version control
# 2. Use strong passwords for production
# 3. Generate a unique encryption key for production
# 4. Configure CORS_ORIGINS with your actual domain(s)
# 5. Adjust APP_WORKERS based on your server CPU cores
